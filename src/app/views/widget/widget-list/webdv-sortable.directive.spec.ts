import { WebdvSortableDirective } from './webdv-sortable.directive';

describe('WebdvSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new WebdvSortableDirective(null);
    expect(directive).toBeTruthy();
  });
});
